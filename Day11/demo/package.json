{
  "name": "demo",
  "version": "1.0.0",
  "description": "## Node JS: * a js runtime eniveronment * uses v8 engine to run js code outside the browser  * we will use it to run our server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.0"
  }
}
